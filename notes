do zrobienia:

semantyka:
hindley milner
    v literały
    v zmienne
    v argumenty function calli
    v explicite podane typy w ciele funkcji
    v explicite podane typy w parametrach funkcji
    v wartość zwracana function calli
        v globalne środowisko
        v zmienne pokazują na siebie nawzajem a nie są usuwane
        v rzecz -> t0 -> t5 -> t11 -> Liczba
    v customowe typy
        v getter chainy
            zacznij od końca
            ustal warianty n-tego elementu
            dla każdego wariantu:
                cofaj się po 1 elemencie getter chaina
                patrz czy da się stypować
            potrzebujemy 1 lub więcej poprawnych możliwości, ale nie konkretyzacji
            update logiki unifikacji: Pies|Człowiek ^ Pies|Żyrafa = Pies
        v struct creation
        v typy parametryzowane (Lista, Mapa)
        v typy wariantowe

        x subscript

        
    v double check fixpoint upper bound


v jest/są
v inaczej w matchach
v redis
v gerundium (?) polubienie/polubić
v literały Prawda/Fałsz
v tryb przypuszczający - usunąłbyś_komentarz dla usera z identyfikatorem?
v funkcja bez zwróć ma typ zwracany wolny (nie Nic)
v jakieś includy

v funkcje, które nic nie zwracają w ciele funkcji (gołe wywołania) muszą się unifikować z Nic

uruchamianie

stdlib:
v Przełącznik
v konkatenacja stringów


które
kiedy
v funkcje wyższego rzędu: zrób/zrobiłbyś/zrobienie

v kolekcja nie może być liczbą mnogą nazwy w zasięgu — ogniwa (lista) obok ogniwo (parametr) skleja się w zasięgu typów przez odczyt dopełniaczowy lp; objaw to occurs check
v Sukcesem z wartością jako żeton
jedna piąta
nazwa użytkownika równa nazwie kandydata

v Tekst to Lista Znaków
v diagnostyka
v brak dzielenia (operatory to tylko plus/minus/razy + porównania);

v Kolejność pól konstrukcji wpływa na wynik inferencji
v input/output do pliku
chr/ord do przygrywki - i guess
v ulepszyć przygrywkę: brak mapy, filtra, folda (mimo że język ma wartości funkcyjne i zastosuj!), indeksowania listy, pokazać liczbę -> Tekst. Auto-include przygrywki też by zdjął rytuał z każdego pliku