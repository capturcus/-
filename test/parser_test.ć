# Stuby funkcji wymagane przez signature-aware fn-call.
# Uniwersalna sygnatura dla `pisać` i `zapisać_token` (w bodies wywołania
# z różną arity): 3 sloty no-prep + slot per stand. przyimek.
aby pisać b c d od e dla f do g w h na i przez j z k o l po m:
    zwrócić

aby weryfikować coś:
    zwrócić

aby alokować_bufor:
    zwrócić

aby stworzyć_gniazdo:
    zwrócić

aby stworzyć_token:
    zwrócić

aby stworzyć_użytkownika:
    zwrócić

aby opakować coś od nadawcy:
    zwrócić

aby przekazać pakiet kanałem do odbiorcy:
    zwrócić

aby zapisać_token w globalnej_mapie dla użytkownika:
    zwrócić

aby zalogować b c d od e dla f do g w h na i przez j z k o l po m:
    zwrócić

aby zawierać b c d od e dla f do g w h na i przez j z k o l po m:
    zwrócić

aby usunąć b c d od e dla f do g w h na i przez j z k o l po m:
    zwrócić

aby zaktualizować b c d od e dla f do g w h na i przez j z k o l po m:
    zwrócić

aby policzyć rzeczy:
    zwrócić

aby zweryfikować b c d:
    zwrócić


aby działać:
    liczba to 5

aby nasrać_w_kiblu:
    gówno to inne_gówno
    zmienna to 78
    tekst to "siemka"

aby dodać_liczby:
    wynik to 2 + 3

aby odjąć_liczby:
    wynik to 5 - 2

aby skomplikowanie_zarządzić_liczby:
    wynik to 2 + 3 * 5

aby podzielić_liczby:
    liczba to 19 / 6

aby rozjebać_parser:
    liczba to -5

aby testować_porównanie:
    wynik to 2 + 3 < 4 + 5

aby testować_ify:
    jeśli x > 3:
        liczba to 1

aby testować_ify_2:
    jeśli x < 3:
        liczba to 1
    inaczej:
        liczba to 2

aby pracować:
    dopóki a < 10:
        jeśli a = 5:
            x to 1
        a to a + 1

# Funkcje z parametrami

aby pisać_coś_do_klienta coś do klienta:
    weryfikuj coś

aby czytać_z_klienta z klienta:
    bufor to alokuj_bufor

aby nasłuchiwać_na_porcie na porcie:
    sock to stwórz_gniazdo

aby wysłać coś do odbiorcy przez kanał od nadawcy:
    pakiet to opakuj coś od nadawcy
    przekaż pakiet kanałem do odbiorcy

aby zalogować_użytkownika użytkownika hasłem:
    weryfikuj hasło
    token to stwórz_token
    zapisz_token w globalnej_mapie dla użytkownika

# Wywołania funkcji

pisz "witaj, świecie"

anna to stwórz_użytkownika
hasło to "dupa.8"
zaloguj annę hasłem

aby reagować na zdarzenie:
    jeśli zdarzenie = "klik":
        pisz "kliknięto"
    inaczej:
        pisz "ignoruję"

aby przestać_obserwować użytkownika (Użytkownik) przez obserwującego (Użytkownik) -> Wynik:
    jeśli zawiera użytkownik obserwującego:
        zwróć fałsz
    usuń obserwującego z obserwujących_użytkownika
    usuń użytkownika z obserwowanych_obserwującego
    zaktualizuj użytkownika w bazie
    zaktualizuj obserwującego w bazie
    zwróć prawda

aby testować_logikę:
    wynik to policzyć rzeczy > 0

definicja Sesji:
    token (Tekst)
    użytkownik (Użytkownik)
    data_utworzenia (Data)
    data_wygaśnięcia (Data)
    adres_ip (AdresIP)

aby zweryfikować_sesję:
    zweryfikuj token sesji
    token sesji to 5
