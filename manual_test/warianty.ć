# Typy wariantowe:
# - deklaracja unii: `Rezultat to Wynik albo Błąd` (warianty to istniejące
#   struktury, bez parametrów typu),
# - dopasowanie: `X jest:` z wyczerpującymi gałęziami
#   `Wariantem z polem [z polem...]:` (narzędnik orzecznika) dekonstruującymi
#   wariant.

definicja Błędu:
    opis (Tekst)

definicja Wyniku z elementem:
    plon (element)

Rezultat to Wynik albo Błąd

# Funkcje zewnętrzne — sygnatury w całości jawne (brak ciała do inferencji).
można wypisać tekst (Tekst) -> Nic
można wysłać tekst (Tekst) do wtyczki (Liczba) -> Liczba

# Gałęzie zwracają różne warianty jednej unii — funkcja jest otypowana unią.
aby zapisać do bazy tekst -> Rezultat:
    kod to wyślij tekst do bazy
    jeśli kod równe zero:
        zwróć Wynik o plonie zero
    inaczej:
        zwróć Błąd o opisie "nie udało się zapisać"

# Dopasowanie `jest:` na nieotypowanym parametrze — unia wnioskowana
# z gałęzi trafia do sygnatury funkcji. Gałąź nie musi wiązać wszystkich
# pól wariantu (`Wynikiem:` bez `z plonem`).
aby opisywać rezultat -> Tekst:
    gdy rezultat jest:
        Wynikiem:
            zwróć "powodzenie"
        Błędem z opisem:
            zwróć opis

aby działać:
    baza to siedem
    rezultat to zapisz do bazy "test"
    gdy rezultat jest:
        Błędem z opisem:
            wypisz "błąd"
            wypisz opis
        Wynikiem z plonem:
            wypisz "ok"
            jeśli plon równe zero:
                wypisz "zero"
    komunikat to opisuj rezultat
    wypisz komunikat
